# Point PATH at the Rust toolchain on the VFS (no writes to /).
export RUST_ROOT="/mnt/oss/rust"
export PATH="$RUST_ROOT/bin:$PATH"

# Optional: make it obvious which toolchain is active in interactive shells.
export ECO_RUST_TOOLCHAIN="vfs-mnt-oss"
